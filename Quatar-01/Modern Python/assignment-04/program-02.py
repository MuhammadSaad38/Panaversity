def area_of_rectangle(length, width):
    # Step 1: Get the length and width
    length = float(length)
    width = float(width)
    
    # Step 2: Calculate the area by multiplying length and width
    area = length * width
    
    # Step 3: Return the calculated area
    return area
